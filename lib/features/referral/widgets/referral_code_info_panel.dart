import 'package:flutter/material.dart';

/// Placeholder panel — code validation is now format-only (no API per keystroke).
/// Kept as a no-op widget so existing import references still compile.
class ReferralCodeInfoPanel extends StatelessWidget {
  const ReferralCodeInfoPanel({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
